$("#link-<%= @link.id %>").remove();

$(`input[name*="[links_attributes]"][value="<%= @link.id %>"]`).each(function(){
  const fields = this.closest(".nested-fields");
  if (fields) fields.remove();
});

